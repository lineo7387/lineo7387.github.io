---
interface Props {
  currentPage: number;
  totalPages: number;
  baseUrl: string;
  category?: string;
}

const {
  currentPage,
  totalPages,
  baseUrl,
  category = "",
} = Astro.props;

const hasPrev = currentPage > 1;
const hasNext = currentPage < totalPages;

const getPageUrl = (page: number) => {
  if (category) {
    return page === 1 ? `${baseUrl}/${category.toLowerCase()}` : `${baseUrl}/${category.toLowerCase()}/${page}`;
  }
  return page === 1 ? baseUrl : `${baseUrl}/${page}`;
};

// Generate page numbers to show
const pages: (number | "...")[] = [];
if (totalPages <= 7) {
  for (let i = 1; i <= totalPages; i++) pages.push(i);
} else {
  pages.push(1);
  if (currentPage > 3) pages.push("...");
  for (
    let i = Math.max(2, currentPage - 1);
    i <= Math.min(totalPages - 1, currentPage + 1);
    i++
  ) {
    pages.push(i);
  }
  if (currentPage < totalPages - 2) pages.push("...");
  pages.push(totalPages);
}
---

{
  totalPages > 1 && (
    <nav class="flex items-center justify-center gap-2" aria-label="Pagination">
      {/* Previous */}
      {hasPrev ? (
        <a
          href={getPageUrl(currentPage - 1)}
          class="px-4 py-3 bg-surface-raised border-2 border-foreground font-bold text-sm uppercase text-text shadow-hard-sm hover:bg-muted dark:hover:bg-muted transition-colors btn-press flex items-center gap-2"
        >
          <svg
            class="w-4 h-4"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="square"
              stroke-width="3"
              d="M19 12H5m7-7l-7 7 7 7"
            />
          </svg>
          Prev
        </a>
      ) : (
        <span class="px-4 py-3 bg-muted border-2 border-foreground font-bold text-sm uppercase text-foreground cursor-not-allowed flex items-center gap-2">
          <svg
            class="w-4 h-4"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="square"
              stroke-width="3"
              d="M19 12H5m7-7l-7 7 7 7"
            />
          </svg>
          Prev
        </span>
      )}

      {/* Page numbers */}
      {pages.map((page) =>
        page === "..." ? (
          <span class="px-2 py-3 font-bold text-muted">...</span>
        ) : (
          <a
            href={getPageUrl(page)}
            class={`px-4 py-3 border-2 border-foreground font-bold text-sm uppercase transition-colors btn-press
            ${
              page === currentPage
                ? "bg-brand-yellow text-on-brand-yellow shadow-hard-sm"
                : "bg-surface-raised text-text hover:bg-muted dark:hover:bg-muted shadow-hard-sm"
            }`}
          >
            {page}
          </a>
        ),
      )}

      {/* Next */}
      {hasNext ? (
        <a
          href={getPageUrl(currentPage + 1)}
          class="px-4 py-3 bg-surface-raised border-2 border-foreground font-bold text-sm uppercase text-text shadow-hard-sm hover:bg-muted dark:hover:bg-muted transition-colors btn-press flex items-center gap-2"
        >
          Next
          <svg
            class="w-4 h-4"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="square"
              stroke-width="3"
              d="M5 12h14m-7-7l7 7-7 7"
            />
          </svg>
        </a>
      ) : (
        <span class="px-4 py-3 bg-muted border-2 border-foreground font-bold text-sm uppercase text-foreground cursor-not-allowed flex items-center gap-2">
          Next
          <svg
            class="w-4 h-4"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="square"
              stroke-width="3"
              d="M5 12h14m-7-7l7 7-7 7"
            />
          </svg>
        </span>
      )}
    </nav>
  )
}
